= render 'navigation'

#landing-page
  #landing-wrap
    = render 'header'

    #landing-main
      -# NOTE: Enable this if some organization support us
      -# = render 'organizations-support-us'

      = render 'mechanisms'

      -# NOTE: Counters (future)
      -# = render "counters"

      = render 'benefits'

      = render 'features'

      -# NOTE: Banner register (future)
      -# = render 'banner-register'

      -# NOTE: Disabled
      -# = render 'faqs'

      -# NOTE: Disabled until we develop this feature
      -# = render 'newsletter-form'

  = render 'footer'
